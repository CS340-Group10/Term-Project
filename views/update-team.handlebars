<!DOCTYPE html>
<li class = "field"><a href = "/admin" id = "addLink"><<< Back</a></li>
<br>

      <h2>Update Team {{team.team_name}}</h2>   
       <form id="update-team" action="/admin" method="post">
        	Team Name:<br><input type="text" name="t_name" value="{{team.team_name}}"><br>
        	City:<br><input type="text" name="city" value="{{team.city}}"><br>
        	State:<br><input type="text" name="state" value="{{team.state}}"><br>
        	Sport Type:<br> <select name="sport_type" id="sportselection" value="{{team.sport}}">
				{{#each sports}}
					<option value="{{sport_id}}">{{sport_name}}</option>
				{{/each}}
			</select><br>
			Revenue:<br><input type="number" name="revenue" value="{{team.revenue}}"><br>
			Active Salary Cap:<br><input type="number" name="active_cap" value="{{team.active_salary_cap}}"><br>
			Signed:<br><input type="number" name="signed" value="{{team.signed}}"><br>
			</select><br>
        	<br><button onclick="updateTeam({{team.team_id}})">Update</button><br>
       </form> 

<script defer>selectSport({{team.sport}});</script>
